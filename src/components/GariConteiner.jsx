

function GariConteiner({children}) {
  return (
    <div className={` pt-6.5 hover:scale-90 transform transition flex flex-col gap-2`}>
      {children}

      
    </div>
  )
}

export default GariConteiner
